name: My First Workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Welcome message
        run: echo "Welcome to First Github Actions jobs"

      - name: List files
        run: ls

      - name: Read file
        run: cat README.md
