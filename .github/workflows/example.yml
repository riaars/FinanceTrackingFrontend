name: My First Workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: List and Read File
        run: |
          echo "Welcome to First Github Actions jobs"
          ls -ltra
          cat README.md

    #   - name: List files
    #     run: ls

    #   - name: Read file
    #     run: cat README.md
